aside.record-list.side-slider
	button.btn-icon-only.close(type="button", aria-hidden="true") ×
	h3= t('record-list.title')
	p= t('record-list.msg1')
	progress.record-list__upload-progress
	ul.record-list__records
	.record-list__button-bar
		button.record-list__button-bar__button.upload.btn.btn-primary(type="button", disabled)= t('record-list.upload') 
		button.record-list__button-bar__button.export.btn.btn-default(type="button", disabled)= t('record-list.export')
	- var uploadGuidance = (draftEnabled) ? t('record-list.msg2', {icon: '<span class="icon icon-pencil"> </span>', interpolation: {escapeValue: false}}) : t('record-list.msg2-nodraft')
		p!= uploadGuidance + ' ' + t('record-list.msg3')
	p.side-slider__app-version= t('version') + ' '
		span.side-slider__app-version__value
	p.side-slider__advanced.hide
		button.side-slider__advanced__button.flush-db.btn.btn-default(type="button")= t('advanced.flushdb')
button.side-slider__toggle.open(type="button", aria-label="open")
button.side-slider__toggle.close(type="button", aria-label="close")
div.slider-overlay
