extends layout

block style
  link(rel='stylesheet', href='/css/error.css') 

block content
  h1.error= message
    if error.status
      span.status-code Error Code: #{error.status}
  if error.stack
    pre #{error.stack}
